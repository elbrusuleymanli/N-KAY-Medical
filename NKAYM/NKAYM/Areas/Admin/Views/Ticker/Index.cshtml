@model List<NewsRun>
@{
    ViewData["Title"] = "Index";
}

<div class="mdc-layout-grid__cell stretch-card mdc-layout-grid__cell--span-12 ">
    <div class="mdc-card p-0 ">
        <div class="topic">

            <h6 class="card-title card-padding pb-0" style="color:white;font-size:40px;text-align:center; font-weight:600;">MƏLUMAT</h6>
           
        </div>
        <div class="table-responsive">
            <table class="table table-striped table-bordered" style="border-color: black; text-align: center">
                <thead>
                    <tr style="background-color: #f7f7f7;text-align:center">
                        <th>MƏTN</th>
                      
                        <th>HƏRƏKƏTLƏR</th>

                    </tr>
                </thead>
                <tbody>
                     
                    <tr>
                        @foreach (var item in Model)
                        {
                            <td>@item.Announcement</td>
                       
                        <td>
                            <a class="btn btn-warning mt-2" style="width:90px" asp-controller="Ticker" asp-action="Update" asp-route-id="@item.Id">Redaktə</a>

                        </td>
                        }
                    </tr>
                    


                </tbody>
            </table>
        </div>
    </div>
</div>

